# Bootstrap Local Tools

Install or verify:

- Terraform or OpenTofu
- doctl if used
- cloudflared if using tunnel mode
- Docker / Docker Compose
- GitHub CLI if used
- jq if used by scripts

Run:

```bash
terraform version || tofu version
docker version
docker compose version
```
