# User Flow Map

First launch → hatch → reveal → main house care → profile/stats → attempt leave house → account prompt → signup/migration → map → adventure → loot → inventory → home customization → progression.
