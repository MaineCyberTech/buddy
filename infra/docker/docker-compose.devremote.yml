name: buddy-dev

services:
  caddy:
    image: caddy:2-alpine
    container_name: buddy-caddy-dev
    restart: unless-stopped
    mem_limit: 64m
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - ./certs:/etc/caddy/certs:ro
      - caddy-data:/data
      - caddy-config:/config
    networks:
      - buddy-dev
    depends_on:
      - app

  app:
    image: ${GHCR_IMAGE_PREFIX}/buddy-app:${IMAGE_TAG}
    container_name: buddy-app-dev
    restart: unless-stopped
    mem_limit: 256m
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_SUPABASE_URL=${NEXT_PUBLIC_SUPABASE_URL:-}
      - NEXT_PUBLIC_SUPABASE_ANON_KEY=${NEXT_PUBLIC_SUPABASE_ANON_KEY:-}
    networks:
      - buddy-dev

networks:
  buddy-dev:
    driver: bridge

volumes:
  caddy-data:
  caddy-config: